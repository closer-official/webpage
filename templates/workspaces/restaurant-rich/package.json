{
  "name": "@webpage-ws-restaurant-rich",
  "private": true,
  "type": "module",
  "description": "飲食店（リッチ）テンプレ作業用（未登録）。deploy / build はリポジトリルートの npm スクリプトを実行します。",
  "scripts": {
    "deploy": "npm --prefix ../../.. run deploy",
    "build": "npm --prefix ../../.. run build",
    "dev": "npm --prefix ../../.. run dev",
    "server": "npm --prefix ../../.. run server"
  }
}
