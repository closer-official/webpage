{
  "name": "@webpage-ws-navy-cyan-web",
  "private": true,
  "type": "module",
  "description": "テンプレ3 navy_cyan_consult 作業用。deploy / build はリポジトリルートの npm スクリプトを実行します。",
  "scripts": {
    "deploy": "npm --prefix ../../.. run deploy",
    "build": "npm --prefix ../../.. run build",
    "dev": "npm --prefix ../../.. run dev",
    "server": "npm --prefix ../../.. run server"
  }
}
